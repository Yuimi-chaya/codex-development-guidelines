# Policy Updates

The canonical [update workflow](../skills/adopt-agent-policy/references/update.md) is bundled with the adoption Skill.

Only deliberate configuration or source updates trigger it, not a new coding thread. Compare the pinned source and local customizations, revisit only changed decisions, and never treat an upstream revision as permission to overwrite user instructions.
