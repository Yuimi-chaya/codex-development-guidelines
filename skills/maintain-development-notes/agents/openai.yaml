interface:
  display_name: "维护开发笔记"
  short_description: "先读取既有项目记忆，再维护复杂项目的可恢复开发笔记"
  default_prompt: "Use $maintain-development-notes to read applicable development notes before acting, reconcile them with current evidence, and maintain an accurate recovery record."

policy:
  allow_implicit_invocation: true
